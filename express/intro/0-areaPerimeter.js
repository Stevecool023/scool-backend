#!/usr/bin/node

const square = require('./0-square');
console.log(`Area of a square with the width of 4 is ${square.area(4)}`);
console.log(`Perimeter of a square with the width of 5 is ${square.perimeter(5)}`);
